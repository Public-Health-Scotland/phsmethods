on:
  push:
    branches: [main, master]
  pull_request:
    types: [opened, reopened, ready_for_review, auto_merge_enabled]
  merge_group: 
  workflow_dispatch:

name: phs_package_checks.yaml

permissions: 
  contents: read
  pull-requests: read
  statuses: read

jobs:
  PHS-checks:
    uses: Public-Health-Scotland/actions/.github/workflows/phs_package_checks.yaml@v2.7.0
    permissions: 
      contents: write
      pull-requests: write
      statuses: write
    secrets: inherit
