on:
  push:
    branches: [main, master]
  pull_request:
  workflow_dispatch:

name: phs_package_checks.yaml

permissions: 
  contents: read
  pull-requests: read
  statuses: read

jobs:
  PHS-checks-test2:
    uses: Public-Health-Scotland/actions/.github/workflows/phs_package_checks.yaml@fix-check-no-suggests
    permissions: 
      contents: write
      pull-requests: write
      statuses: write
    secrets: inherit
